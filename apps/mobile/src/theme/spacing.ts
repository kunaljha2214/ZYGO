/** Stitch Zygo Neon Dark — spacing scale (8pt grid). */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
  /** Horizontal screen gutter */
  screenX: 20,
  /** Below status bar / below stack header */
  screenTop: 8,
  screenTopTab: 12,
  /** Above tab bar / home indicator */
  screenBottom: 28,
  section: 24,
  cardGap: 12,
  stackGap: 16,
} as const;
