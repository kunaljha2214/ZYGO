import React from 'react';
import { RoleProfileScreen } from './profile/RoleProfileScreen';

/** Partner / admin account tab — same Profile UI as customer. */
export function AccountScreen() {
  return <RoleProfileScreen />;
}
